ansible runs on push based mechanism and agentless and jinja used to create templates
vim .ssh/authorized_keys give public key in worker nodes, key generate in control node using ssh-keygen. We can ssh to worker from master
ansible servers -a "free -h"
ansible servers -a "uname -r"
ansible servers -a "sudo apt update"
ansible-inventory --list
ansible-playbook nginx.yml -v
ansible.cfg file -->  to set ansible opern and defaults, central configuration file used to define settings and parameters that alter how Ansible operates
ansible fact --> informn about a managed node, discovered by ansible
run pb against specific set of hosts -->  use --limit option
 ansible servers -m "setup"    - informn regarding gathering facts> node informn, ansible servers -m "setup" | grep -i "distribution"
